spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Appfinance
    username: postgres
    password: Diegocosta32
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  jpa.properties.hibernate.format_sql: true